import { signIn, signInWithGoogle, signUp } from "./actions";

export default async function SignInPage({ searchParams }: { searchParams: Promise<{ error?: string; message?: string }> }) {
  const query = await searchParams;
  return <div className="mx-auto max-w-md px-5 py-16"><p className="text-xs font-bold uppercase tracking-[.18em] text-black/40">Runned account</p><h1 className="mt-2 text-4xl font-black tracking-[-.045em]">Build your runner profile.</h1><p className="mt-3 text-sm leading-6 text-black/55">Authentication is wired for Supabase. Add environment credentials to activate it.</p>{query.error && <p className="mt-5 rounded-2xl bg-red-50 p-3 text-sm text-red-700">{query.error}</p>}{query.message && <p className="mt-5 rounded-2xl bg-emerald-50 p-3 text-sm text-emerald-700">{query.message}</p>}<form className="mt-8 space-y-3"><input name="email" type="email" required placeholder="Email" className="w-full rounded-2xl border border-black/10 bg-white px-4 py-3 outline-none"/><input name="password" type="password" minLength={8} required placeholder="Password" className="w-full rounded-2xl border border-black/10 bg-white px-4 py-3 outline-none"/><div className="grid grid-cols-2 gap-2"><button formAction={signIn} className="rounded-2xl bg-black px-4 py-3 text-sm font-bold text-white">Sign in</button><button formAction={signUp} className="rounded-2xl border border-black/10 bg-white px-4 py-3 text-sm font-bold">Create account</button></div></form><form action={signInWithGoogle} className="mt-3"><button className="w-full rounded-2xl border border-black/10 bg-white px-4 py-3 text-sm font-bold">Continue with Google</button></form></div>;
}
